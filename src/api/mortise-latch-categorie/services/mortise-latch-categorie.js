'use strict';

/**
 * mortise-latch-categorie service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::mortise-latch-categorie.mortise-latch-categorie');

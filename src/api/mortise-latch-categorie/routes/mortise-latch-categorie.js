'use strict';

/**
 * mortise-latch-categorie router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::mortise-latch-categorie.mortise-latch-categorie');

'use strict';

/**
 * pull-handles-product-categorie router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::pull-handles-product-categorie.pull-handles-product-categorie');

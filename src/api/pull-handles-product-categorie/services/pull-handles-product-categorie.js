'use strict';

/**
 * pull-handles-product-categorie service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::pull-handles-product-categorie.pull-handles-product-categorie');

'use strict';

/**
 * pull-handles-product-categorie controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::pull-handles-product-categorie.pull-handles-product-categorie');
